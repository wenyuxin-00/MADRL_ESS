================================================================================
            MADRL_ESS - 完整多智能体执行流程图
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                     MAIN ENTRY: TrainRunner.run()                           │
│                   (runners/train_runner.py, lines 124-249)                   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ INITIALIZATION PHASE                                                         │
│  • Create Agents: agent_n = [MADDPG(cfg, id) for id in range(num_agents)]   │
│  • Create Vectorized Environment: env = DummyVecEnv(num_envs, ...)          │
│  • Initialize ReplayBuffer                                                  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ MAIN TRAINING LOOP (for target_interactions steps)                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
        ┌─────────────────────────────┼─────────────────────────────┐
        │                             │                             │
        ▼                             ▼                             ▼
   ┌─────────┐              ┌─────────────────┐            ┌──────────┐
   │  STEP 1 │              │  STEP 2 & 3     │            │  STEP 4  │
   │ RESET   │              │ SELECT & FORMAT │            │  PARAM   │
   │   ENV   │              │  ACTIONS        │            │ UPDATE   │
   └─────────┘              └─────────────────┘            └──────────┘
        │                             │                         │
        └─────────────┬───────────────┴─────────────────────────┘
                      │
                      ▼
        ┌─────────────────────────────────┐
        │   CONTINUE UNTIL CONVERGENCE    │
        └─────────────────────────────────┘


================================================================================
                    STEP 1: ENVIRONMENT RESET
================================================================================

env.reset()
    │
    ├─ DummyVecEnv.reset() [common/vec_env.py, line 39-41]
    │   │
    │   └─ for env in self.envs:
    │       │
    │       ├─ EnergyStorageEnv.reset() [envs/hems_env.py, line 128-145]
    │       │   │
    │       │   ├─ Load episode data (price, load, pv signals)
    │       │   ├─ Reset SOC to initial value
    │       │   └─ obs = obs_builder.build(self)
    │       │       │
    │       │       └─ returns structured obs dict:
    │       │           {local: (n_agents, local_dim),
    │       │            global: (...),
    │       │            ...}
    │       │
    │       └─ append to obs_list
    │
    └─ stack_nested(obs_list)
        │
        └─ returns batched_obs:
            {local: (num_envs, n_agents, local_dim), ...}


================================================================================
                    STEP 2: SELECT ACTIONS
================================================================================

action_batch = TrainRunner.select_action_batch(obs_np)
[runners/train_runner.py, lines 67-75]
        │
        ├─ obs_t = to_torch_nested(obs_np, device)
        │   │
        │   └─ convert numpy dict to torch dict on device
        │
        └─ action_t = torch.stack([
               agent.act_from_torch_obs(obs_t, noise_std)
               for agent in agent_n
           ], dim=1)
           │
           ├─ for each agent:
           │   │
           │   └─ MADDPG.act_from_torch_obs(obs_t, noise_std)
           │       [algorithms/maddpg.py, lines 57-61]
           │       │
           │       ├─ action = self.actor(obs_t)  ← FORWARD PASS
           │       │   │
           │       │   └─ torch tensor (batch, action_dim)
           │       │
           │       ├─ if noise_std > 0:
           │       │   action += torch.randn_like(action) * noise_std
           │       │
           │       └─ return action.clamp(-max_action, max_action)
           │           │
           │           └─ torch tensor (batch, action_dim)
           │
           └─ torch.stack along dim=1
               │
               └─ shape: (num_envs, n_agents, action_dim)
        │
        └─ return action_t.cpu().numpy()
            │
            └─ numpy array (num_envs, n_agents, action_dim)


================================================================================
                    STEP 3: FORMAT AND EXECUTE ENV
================================================================================

formatted_actions = TrainRunner.format_env_actions(action_batch)
[runners/train_runner.py, lines 63-65]
        │
        └─ reshape to List[n_agents] of (num_envs, action_dim)


next_obs, reward, done, info_list = env.step(formatted_actions)
[common/vec_env.py, lines 43-61]
        │
        ├─ for env_idx, env in enumerate(self.envs):
        │   │
        │   ├─ action_n = split_batched_actions(actions_n_batched, env_idx, ...)
        │   │   [common/vec_env.py, lines 10-15]
        │   │   │
        │   │   └─ return [np.asarray(actions_n_batched[agent_id][env_idx])
        │   │              for agent_id in range(num_agents)]
        │   │       │
        │   │       └─ List[n_agents] of (action_dim,)
        │   │
        │   ├─ obs, reward, done, info = env.step(action_n)
        │   │   │
        │   │   └─ EnergyStorageEnv.step(actions: List[np.ndarray])
        │   │       [envs/hems_env.py, lines 223-312]
        │   │       │
        │   │       ├─ 1. ACTION PROCESSING [lines 226-228]
        │   │       │   action_array = np.asarray(actions).reshape(...)
        │   │       │   action_array = np.clip(action_array, -1.0, 1.0)
        │   │       │   e_bat_req = action_array * agent_p_max
        │   │       │
        │   │       ├─ 2. BATTERY CONSTRAINTS [lines 234-245]
        │   │       │   p_max_chg = max charging power
        │   │       │   p_max_dis = max discharging power
        │   │       │   e_bat = np.clip(e_bat_req, p_lower, p_upper)
        │   │       │
        │   │       ├─ 3. STATE UPDATE [lines 247-249]
        │   │       │   delta_e = energy change
        │   │       │   e_next = updated energy
        │   │       │   soc_next = updated SOC
        │   │       │
        │   │       ├─ 4. SIGNAL EXTRACTION [lines 251-256]
        │   │       │   price_t = get_signal_step("price", t)
        │   │       │   load_t = get_signal_step("load", t)
        │   │       │   pv_t = ep_pv[t]
        │   │       │   net_load_t = load_t - pv_t
        │   │       │
        │   │       ├─ 5. REWARD CALCULATION [line 276]
        │   │       │   reward, components = reward_fn.compute(env_state)
        │   │       │
        │   │       ├─ 6. OBSERVATION BUILDING [line 284]
        │   │       │   obs = obs_builder.build(self) if not done else zeros
        │   │       │
        │   │       └─ return (obs, reward, done_n, info)
        │   │
        │   ├─ if all(done):
        │   │   obs = env.reset()
        │   │
        │   └─ append to lists (obs_list, reward_list, done_list, info_list)
        │
        └─ stack_step_outputs(obs_list, reward_list, done_list, info_list)
            [common/vec_env.py, lines 18-23]
            │
            ├─ batched_obs = stack_nested(obs_list)
            ├─ batched_reward = np.stack(reward_list)
            ├─ batched_done = np.stack(done_list)
            │
            └─ return (batched_obs, batched_reward, batched_done, info_list)
                │
                └─ all batched and ready for next iteration


================================================================================
                    STEP 4: STORE & UPDATE
================================================================================

STORAGE [runners/train_runner.py, line 172]
├─ replay_buffer.store_transitions_batched(obs, action_batch, reward,
                                            next_obs, done)
│   │
│   └─ store experience for later learning


PARAMETER UPDATE [runners/train_runner.py, lines 206-217]
(Every update_interval steps, when buffer has enough samples)
│
├─ batch_np = replay_buffer.sample()
│   │
│   └─ random batch from stored experiences
│
├─ batch_torch = to_torch_batch(batch_np, device)
│   │
│   └─ convert batch to torch tensors on device
│
└─ for agent in agent_n:
   │
   └─ agent.train_on_batch(batch_torch, agent_n)
       │
       └─ MADDPG.train_on_batch(batch, agent_n)
           [algorithms/maddpg.py, lines 67-103]
           │
           ├─ CRITIC UPDATE
           │   ├─ next_action = stack([agent.actor_target(next_obs) for agent])
           │   ├─ target_q = reward + gamma * (1-done) *
           │   │             critic_target(next_obs, next_action)
           │   ├─ current_q = critic(obs, action)
           │   ├─ critic_loss = MSE(current_q, target_q)
           │   ├─ backward & optimizer.step()
           │   └─ clip gradients if configured
           │
           ├─ ACTOR UPDATE
           │   ├─ new_action = action.clone()
           │   ├─ new_action[:, agent_id] = actor(obs)
           │   ├─ actor_loss = -critic(obs, new_action).mean()
           │   ├─ backward & optimizer.step()
           │   └─ clip gradients if configured
           │
           └─ TARGET NETWORK SOFT UPDATE
               ├─ for critic: param_target = tau * param + (1-tau) * param_target
               └─ for actor: param_target = tau * param + (1-tau) * param_target


NOISE DECAY [runners/train_runner.py, line 204]
└─ if use_noise_decay:
    noise_std = max(noise_std - decay, min_noise)


================================================================================
                    DATA FLOW: SHAPES & TRANSFORMATIONS
================================================================================

OBSERVATION FLOW:
─────────────────
EnergyStorageEnv.reset()
└─ {local: (n_agents, local_dim), global: (...), ...}
                                                    │
    DummyVecEnv.reset()                            │
    ├─ stack_nested([obs1, obs2, ...])             │
    └─ {local: (num_envs, n_agents, local_dim), ...} ◄─ BATCHED


ACTION FLOW:
────────────
TrainRunner.select_action_batch(obs_np)
├─ to_torch_nested(obs_np)
│  └─ {local: torch (num_envs, n_agents, local_dim), ...}
│
├─ agent.act_from_torch_obs(obs_t, noise_std)
│  ├─ self.actor(obs_t)
│  │  └─ torch (num_envs, n_agents, action_dim) ◄─ ACTOR OUTPUT
│  │
│  └─ + noise, clamp
│     └─ torch (num_envs, n_agents, action_dim)
│
├─ torch.stack([...], dim=1)
│  └─ torch (num_envs, n_agents, action_dim)
│
└─ .cpu().numpy()
   └─ numpy (num_envs, n_agents, action_dim)

TrainRunner.format_env_actions(action_batch)
├─ reshape to List[n_agents]
└─ [action1, action2, ...] each (num_envs, action_dim)

DummyVecEnv.step()
├─ split_batched_actions()
│  └─ [action1, action2, ...] each (action_dim,) ◄─ PER-ENV
│
└─ EnergyStorageEnv.step(action_n)
   ├─ np.asarray(actions).reshape()
   │  └─ (n_agents,)
   │
   ├─ * agent_p_max
   │  └─ (n_agents,) power requests
   │
   └─ battery dynamics & reward
      └─ scalar reward per agent


REWARD FLOW:
────────────
EnergyStorageEnv.step()
├─ reward_fn.compute(env_state)
└─ [r1, r2, ..., r_n] per agent

DummyVecEnv.step()
├─ np.stack([reward_list], axis=0)
└─ (num_envs, n_agents, 1)

TrainRunner.run()
├─ stored in replay_buffer
└─ sampled for training


================================================================================
                    KEY TIMING (milliseconds)
================================================================================

select_action_batch(obs_np):  0.5-2ms
  │
  └─ agent.act_from_torch_obs() × n_agents: depends on network size

env.step(formatted_actions):  1-5ms
  │
  └─ EnergyStorageEnv.step() × num_envs: depends on signal lookups

agent.train_on_batch(batch, agent_n):  5-20ms
  │
  └─ actor + critic loss + backward: depends on batch size & network

Overall training speed: 50-200 steps/sec


================================================================================
                    COMPLETE SINGLE ITERATION
================================================================================

┌─────────────────────┐
│  obs (batched)      │
│ Dim: {local: ...}   │
└──────────┬──────────┘
           │
           ▼
┌──────────────────────────────────┐
│ select_action_batch(obs)         │
│ TrainRunner (67-75)              │
│ ─────────────────────────────────│
│ • to_torch_nested(obs)           │
│ • agent.act_from_torch_obs() x n │
│ • torch.stack + numpy()          │
└──────────┬───────────────────────┘
           │
           ▼ action_batch (num_envs, n_agents, action_dim)
           │
┌──────────────────────────────────┐
│ format_env_actions(action_batch)  │
│ TrainRunner (63-65)               │
└──────────┬───────────────────────┘
           │
           ▼ List[n_agents] of (num_envs, action_dim)
           │
┌──────────────────────────────────┐
│ env.step(formatted_actions)      │
│ DummyVecEnv (43-61)              │
│ ─────────────────────────────────│
│ for each env:                    │
│  • split_batched_actions()       │
│  • env.step(action_n)            │
│    └─ EnergyStorageEnv.step()    │
│  • handle reset if done          │
│ stack_step_outputs()             │
└──────────┬───────────────────────┘
           │
           ▼ (next_obs, reward, done, info_list)
           │
┌──────────────────────────────────┐
│ replay_buffer.store(...)         │
│ TrainRunner (172)                │
└──────────┬───────────────────────┘
           │
    ┌──────┴──────┐
    │             │ (every update_interval steps)
    ▼             ▼
┌─────────┐  ┌──────────────────────┐
│  Loop   │  │ agent.train_on_batch │
│  back   │  │ MADDPG (67-103)      │
│  to     │  │ ──────────────────────│
│  next   │  │ • critic update      │
│ iter    │  │ • actor update       │
│         │  │ • target soft update │
└─────────┘  └──────────────────────┘
                      │
                      └─ parameters updated

================================================================================
